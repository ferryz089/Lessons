def divide(first, second):              # Создаем функцию первого модуля
    if second == 0:                     # условие1
        return 'Ошибка'                 # возвращаем текст
    if second != 0:                     # условие2
        return first/second             # возвращаем результат деления